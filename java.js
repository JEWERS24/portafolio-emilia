document.addEventListener('contextmenu', (e) => e.preventDefault());

document.addEventListener('DOMContentLoaded', () => {
    // Custom cursor and particle features removed.
    // Leave file minimal for future enhancements.
});
